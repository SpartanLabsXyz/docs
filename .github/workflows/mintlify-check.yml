name: Mintlify Broken Links Check

on:
  pull_request:
    branches: [main]

jobs:
  broken-links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check for broken links
        uses: mintlify/broken-links-checker@v1
